#importamos la libreria
import tkinter as tk
#Creamos una instancia de la clase TK. Asi se crea la root window
root = tk.Tk()
#Añadimos algunos widgets
frame = tk.Frame(root)
frame.pack()

#ejecutamos el metodo mainloop, que es el que mantiene la ventana visible
root.mainloop()